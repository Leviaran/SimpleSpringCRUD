##Spring DATASOURCE (Data source auto configuration & data source prpoerties)
spring.datasource.url = jdbc:mysql://localhost:3306/notes_app?useSSL=false
spring.datasource.username = root
spring.datasource.password = toor


##Hibernate properties
# sql dialect makes hibernate generate better sql for choosen database

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

#Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto =  update